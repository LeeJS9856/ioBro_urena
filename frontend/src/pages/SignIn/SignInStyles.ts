import { StyleSheet } from 'react-native';

const SignInStyles = StyleSheet.create({
  logo: {
    marginBottom: 100,
  },
  signUp: {
    color: '#464A4D',
    marginTop: 60,
    fontSize: 12,
    fontWeight: 400,
  },
});

export default SignInStyles;
